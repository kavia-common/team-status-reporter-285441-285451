# Backend environment variables for PostgreSQL connection

# Preferred: provide individual parts (these take precedence if all present)
POSTGRES_HOST=localhost
POSTGRES_PORT=5000
POSTGRES_DB=myapp
POSTGRES_USER=appuser
POSTGRES_PASSWORD=dbuser123

# Optional: full URL. Must include username (and typically password) to be valid.
# If this is malformed (e.g., missing username), the backend will ignore it and
# use the individual parts above.
# POSTGRES_URL=postgresql://appuser:dbuser123@localhost:5000/myapp

# SSL toggle (set to "true" only if your database requires SSL)
POSTGRES_SSL=false

# Bcrypt configuration
BCRYPT_SALT_ROUNDS=10

# App server binding
PORT=3001
HOST=0.0.0.0
